# import-and-export
Export is used to share variables or functions from one JavaScript file.
Import is used to bring those exported items into another file so they can be used there.

It helps keep code organized, reusable, and clean, especially in larger projects.
